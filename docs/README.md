# Screenshots

Drop PNG screenshots here so they appear in the main `README.md`:

- `home.png` — the Home / download page
- `queue.png` — the Queue page with active downloads
- `history.png` — the History table
- `settings.png` — the Settings tabs

Tip: press <kbd>Win</kbd>+<kbd>Shift</kbd>+<kbd>S</kbd> to capture, then save with these names.
